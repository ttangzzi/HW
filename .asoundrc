pcm.!default {
  type asym
  capture.pcm "mic"
  playback.pcm "speaker"
}
pcm.mic {
  type plug
  slave {
    pcm "hw:1,0" // arecord -l 과 aplay -l에서의 card, device 번호에 맞게
  }
}
pcm.speaker {
  type plug
  slave {
    pcm "hw:1,0" // arecord -l 과 aplay -l에서의 card, device 번호에 맞게
  }
}